#bravo/prompts.py

NARRATIVE = """당신은 조세·국제조세·기업세무 분야 판결문을 분석하는 전문 분석관이다.  
입력된 판결문을 **논리적으로 해석하고, 구조화된 JSON**으로 정리하라.

⚠ 절대로 해서는 안 되는 것:
입력된 텍스트 외의 내용을 구조화하지 말 것

=====================================================
[출력 스키마(JSON)]
{format_instructions}
아래 정확한 구조의 JSON을 출력하라:

{{
  "fact_summary": "<판례에서 확인되는 사실관계를 요약한 한 문단>",
  "plaintiff_arguments": ["<원고의 주장 문장 1>", "<원고의 주장 문장 2>"],
  "defendant_arguments": ["<피고의 주장 문장 1>", "<피고의 주장 문장 2>"],
  "legal_context": ["<관련 법리 문장 1>", "<관련 법리 문장 2>"],
  "court_reasoning": ["<법원의 판단 문장 1>", "<법원의 판단 문장 2>"],
  "core_conflicts": ["<텍스트에서 드러난 실제 구체적 다툼 포인트 3~6개>"]
}}

=====================================================
📌 매우 중요한 형식 규칙 (꼭 지킬 것)

1) "fact_summary"  
   - 반드시 문자열(str) 하나여야 한다.  
   - 문단 전체가 하나의 자연스러운 서술로 이어져야 한다.
   - 입력 텍스트의 fact/argument/legal_basis/reasoning 내용을 모아
    사건의 흐름을 자연스럽게 재구성한다.
  - 절대 텍스트에 없는 새로운 사실을 만들지 말 것.

2) "plaintiff_arguments" / "defendant_arguments"
- 텍스트 내 문장 중 '원고는 … 주장한다', '피고는 … 주장한다' 패턴을 기반으로
  각각의 주장만 요약한다.
- 추상적 테마(예: 실질과세 일반론)는 제외한다.

3) "legal_context"
- 텍스트에 등장한 법 조항, 기준, 계산방법(예: 가중평균차입이자율)만 포함.
- 텍스트에 없는 법조문 추가 금지.

4) "court_reasoning"
- 텍스트 내 reasoning_core / court_reasoning 문장을 기반으로
  판단 프로세스만 재배열한다.
- ‘법원은 ~라고 보았다’ 형태로 자연스러운 문장으로 재구성.

5) "core_conflicts"
   - 반드시 텍스트에서 명시적으로 드러난 “구체적 다툼 포인트”만 나열한다.
   - broad topic(예: 실질과세 일반론)의 단독 사용은 금지한다.
   - 반드시 3~6개의 문장 형태로 작성한다.

6) "plaintiff_arguments", "defendant_arguments", "legal_context", "court_reasoning"
   - 반드시 **문장 단위 요소로 구성된 JSON 배열(List[str])**이어야 한다.
   - 하나의 요소는 반드시 “완전한 문장”이어야 하며, “글자 쪼개기”는 절대 불가.
   - 아래와 같은 형태만 허용된다:

     예시:
     "legal_context": [
       "정상이자율 산정 기준은 구 국제조세조정법 제5조 제1항에 따른다.",
       "개정된 시행령의 적용 범위는 해당 회계연도의 규정에 의해 결정된다."
     ]

=====================================================
[입력 TEXT]
{full_text}

=====================================================
"""
KEYWORD = """
당신은 조세·국제조세·기업세무 사건의 쟁점을 분석하는 전문 분석관입니다.
입력된 “쟁점(core_conflict)” 문장을 읽고,  
해당 쟁점을 구성하는 **핵심 개념/용어(Key Terms)**를 1-2개 추출하십시오.

아래 규칙을 반드시 지키십시오:

========================================================
[출력 규칙]
1) 키워드는 반드시 **입력 텍스트 또는 narrative 전체에서 실제 등장 가능한 용어**여야 합니다.
   - 사건명/조문/법리 추가 생성 금지
   - 교과서적 일반 개념 생성 금지

2) 키워드는 문장 단위가 아니라 **단어 또는 짧은 구(phrase)** 형태여야 합니다.
   예:
     - "부당행위계산부인"
     - "장부가액"
     - "가공경비"
     - "부담부증여"
     - "정상이자율 산정"

3) 해당 쟁점을 ‘식별’하거나 ‘구분’하는 데 도움이 되는 표현만 선택하십시오.
   주변적 서술, 폭넓은 경제 개념은 제외하십시오.

4) 출력은 아래 JSON 형식만 허용됩니다:
{format_instructions}

{{
  "keywords": ["...", "...", "..."]
}}

========================================================
[입력 쟁점]
{core_conflict}
"""

CLUSTER = """
아래 키워드들을 의미적으로 유사한 그룹으로 묶어라.
각 그룹마다 대표 키워드 1개를 선택하고,
나머지 키워드들을 해당 대표 키워드에 매핑하라.

⚠️ 주의:
- 반드시 제공된 키워드만 사용하라.
- 새로운 키워드를 생성하지 마라.
- 하나의 키워드는 반드시 한 그룹에만 속하도록 분류하라.

========================================================
출력은 아래 JSON 형식만 허용된다:
{format_instructions}

예시:
{{
  "현물출자": ["현물출자 거래", "현물출자 대가", "현물출자계약"],
  "정상이자율": ["정상이자율 산정", "간주정상이자율"]
}}
========================================================

[입력 키워드 목록]
{keywords}
"""

PASS_ZERO = """
당신의 임무는 full_text 안의 문장들을,
제공된 keywords 리스트를 기준으로 분류하여
issue_groups를 구성하는 것이다.

⚠ issue_groups의 key는 반드시 keywords 리스트의 항목을 참고한다.
⚠ 제공된 full_text 안의 내용을 사용하여 구조화 하라.

=====================================================
[분류 기준]

- keywords 리스트는 본 사건의 핵심키워드를 의미한다.
- full_text 안의 문장에 keyword 참조하여 각 당사자가 다투는 쟁점을 "issue_group"으로 잡는다.
- "core_conflicts"는 해당 issue_group에 해당하는 텍스트에서 드러난 실제 구체적 다툼 포인트로 잡는다.
- 긴 문단의 경우 문장 단위로 나누어 분류한다.

=====================================================
📌 출력 스키마(JSON)
{format_instructions}

{{
  "issue_groups": {{
      "<core_conflicts>": {{
          "plaintiff_arguments": [
              "<full_text에서 해당 쟁점과 관련된 '원고 주장' >"
          ],
          "defendant_arguments": [
              "<full_text에서 해당 쟁점과 관련된 '피고 주장'>"
          ],
          "legal_context": [
              "<full_text에서 해당 쟁점을 판단하기 위해 사용된 법리/판례/해석 >"
          ],
          "court_reasoning": [
              "<full_text에서 해당 쟁점에 대해 법원의 판단 과정>"
          ]
      }},

      "<core_conflicts>": {{
          "plaintiff_arguments": [...],
          "defendant_arguments": [...],
          "legal_context": [...],
          "court_reasoning": [...]
      }}
  }}
}}
=====================================================
📌 문장 분류 규칙

2) "plaintiff_arguments" / "defendant_arguments"
- 텍스트 내 문장 중 '원고는 … 주장한다', '피고는 … 주장한다' 패턴을 기반으로
  각각의 주장만 요약한다.
- 추상적 테마(예: 실질과세 일반론)는 제외한다.

3) "legal_context"
- 텍스트에 등장한 법 조항, 기준, 계산방법(예: 가중평균차입이자율)만 포함.
- 텍스트에 없는 법조문 추가 금지.

4) "court_reasoning"
- 텍스트 내 reasoning_core / court_reasoning 문장을 기반으로
  판단 프로세스만 재배열한다.
- ‘법원은 ~라고 보았다’ 형태로 자연스러운 문장으로 재구성.

5) "core_conflicts"
   - 반드시 텍스트에서 명시적으로 드러난 “구체적 다툼 포인트”만 나열한다.
   - broad topic(예: 실질과세 일반론)의 단독 사용은 금지한다.
   - 반드시 3~6개의 문장 형태로 작성한다.

6) "plaintiff_arguments", "defendant_arguments", "legal_context", "court_reasoning"
   - 반드시 **문장 단위 요소로 구성된 JSON 배열(List[str])**이어야 한다.
   - 하나의 요소는 반드시 “완전한 문장”이어야 하며, “글자 쪼개기”는 절대 불가.
   - 아래와 같은 형태만 허용된다:

     예시:
     "legal_context": [
       "정상이자율 산정 기준은 구 국제조세조정법 제5조 제1항에 따른다.",
       "개정된 시행령의 적용 범위는 해당 회계연도의 규정에 의해 결정된다."
     ]
=====================================================
[제공된 keywords]
{keywords}

[분류할 full_text]
{full_text}
=====================================================
"""

GLOBAL_SUMMARY_TMPL = """
당신의 임무는 full_text 를 기반으로
법원의 논리적 판단 흐름을 IssueLogic 구조로 정리하는 것이다.

⚠ 절대 새로운 사실이나 주장, 법리는 만들지 마라.
⚠ full_text 내부 문장만을 재서술 또는 요약하여 작성하라.

=====================================================
[full_text]
{full_text}

=====================================================
# [출력 스키마(JSON)]
{format_instructions}


{{
  "global_outline": "사건 전체의 판단 흐름을 간결하게 요약한 한 문단",
  "main_issues": [
    "<core_conflict1 을 기반한 쟁점 표현>",
    "<core_conflict2 을 기반한 쟁점 표현>",
    "..."
  ],
    "issue_logic_chains": [
      {{
        "issue": "<core_conflict1>",
        "premise": "사실·개념적 출발점",
        "evidence": "텍스트에서 확인되는 증거·자료",
        "rule": "텍스트에서 확인되는 법령·기준",
        "application": "해당 규정이 어떻게 적용되었는지",
        "inference": "중간 판단 단계",
        "mini_conclusion": "해당 쟁점에서의 법원의 결론"
      }},
      {{
        "issue": "<core_conflict2>",
        "premise": "...",
        "evidence": "...",
        "rule": "...",
        "application": "...",
        "inference": "...",
        "mini_conclusion": "..."
      }}
    ]
}}

=====================================================
📌 구조 생성 규칙 (전역 규칙: GLOBAL RULES)

0) global_outline  
   - 사건 전체 reasoning 흐름을 ‘하나의 문단’으로 요약한다.  
   - full_text 내 정보만 사용한다.  
   - 새로운 법리·외부 배경·추가 의견 금지.

1) main_issues  
   - 쟁점을 “~ 여부” 형태의 문장으로 명확히 표현한다.  
   - core_conflict는 텍스트에서 드러난 구체적 다툼 포인트만 사용한다.

2) issue_logic_chains  
   - 각 쟁점(issue)에 대해 법원이 실제로 밟은 판단 절차를 구조화한다.  
   - “premise → evidence → rule → application → inference → mini_conclusion”  
     순서를 반드시 따른다.  
   - 판사의 판단 흐름을 그대로 재구조화한 것이어야 한다.  
   - 원고·피고 주장이나 과세관청 의견을 inference로 적지 않는다.  
   - 문장 단위로 작성하며, 추상적 교과서 서술 금지.

=====================================================
📌 세부 단계 규칙 (슬롯 정의: SLOT DEFINITIONS)

1) premise  
   - 쟁점 판단을 위해 법원이 전제로 삼은 사실관계 또는 개념적 출발점  
   - 원고·피고의 주장 중 ‘법원이 인정한 사실’ 또는 ‘판단의 기반이 되는 상황’  
   - 판단의 초기 조건

2) evidence  
   - 위 premise를 뒷받침하는 텍스트 내 근거  
   - 법원이 사실관계를 확인할 때 참고하는 자료, 주장, 문서, 정황  
   - 판사가 “이 사실이 맞다” 또는 “이런 사정이 있다”고 평가할 때 인용한 요소

3) rule  
   - 법원이 쟁점 해결을 위해 적용한 법령, 규정, 해석 기준  
   - 법조문, 시행령·시행규칙, 판례 기준 등  
   - 반드시 텍스트에 나타난 규정 또는 기준만 사용

4) application  
   - 위 ‘rule’을 이 사건 특정 사실관계에 어떻게 적용했는지  
   - 법원이 개별 사실에 법 규정을 대입하여 판단을 전개하는 과정  
   - “이 사건의 상황은 rule에 비추어 이렇게 해석된다” 식의 논리

5) inference  
   - rule 적용 이후, 법원이 도출한 중간 판단  
   - 아직 최종 결론은 아님 
   - 법원이 rule을 이 사건 구체적 사실에 어떻게 대입했는지  
   - “이 사건 사실은 rule에 비추어 이렇게 해석된다” 식의 서술,
     “따라서 이 부분은 합리적이지 않다”, “이 점은 인정되기 어렵다” 등  
   - 텍스트 속 법원의 추론 또는 논리적 연결고리를 요약

6) mini_conclusion  
   - 쟁점별로 법원이 도달한 결론  
   - 판결문 reasoning의 해당 이슈 마지막 문단에 해당하는 판단  
   - 최종 판단이 아니라 “이 쟁점에서의 결론”임

=====================================================

"""

CITATION = """

당신은 주어진 텍스트(full_text) 안에서 등장하는 **법령 조문·시행령·시행규칙·판례**를 
정확하게 추출하는 전문 분석관입니다.

당신의 임무는 full_text를 읽고, 아래 스키마에 맞춰 
해당 이슈(issue)에 관련된 citation 목록을 JSON으로 출력하는 것입니다.

=================================================
[출력 규칙 — 매우 중요]

1) citation은 full_text 안에 실제로 등장한 **법령명 + 조문 전체**, 또는 **판례명**만 추출한다.
   - 절대 새 용어를 만들지 말 것.
   - 법령명은 반드시 원문 그대로 적을 것.
   - 조문 번호는 "제00조 제00항 제00호"까지 전체 포함.

2) source_type:
   - 법률, 시행령, 시행규칙, 조문 등 → "statute"
   - 판례, 재결, 법원 판단 인용 → "case"

3) title:
   - 법령/판례의 정확한 명칭을 원문 그대로 적는다.
   예: 
      "구 국제조세조정에 관한 법률 제5조 제1항 제6호"
      "법인세법 시행령 제89조"
      "대법원 2011두8178 판결"

4) citation_text:
   - full_text 안에서 해당 법령 또는 판례가 실제로 등장한 **원문 구절을 그대로 적는다.**
   - 요약하거나 의미를 바꾸지 말 것.

5) promulgation_no / promulgation_type / is_prior_version / effective_before:
   - 법령 인용에 "(XXXX. XX. XX. 대통령령 제XXXXX호로 개정되기 전의 것)" 같은 단서가 있으면:
     * promulgation_no: 숫자만 추출 (예: "19893")
     * promulgation_type: "법률" | "대통령령" | "부령" | "조약" 중 해당하는 것
     * is_prior_version: true
     * effective_before: 괄호 안 날짜를 "YYYY-MM-DD" 형식으로 (예: "2007-02-28")
   - "구 법령명" 패턴만 있고 괄호 단서가 없으면: is_prior_version: true, 나머지 null
   - 단서가 없는 현행 법령: 모두 null / false

6) 최종 JSON 구조는 아래 스키마를 반드시 따른다.

=================================================
[출력 스키마(JSON)]
{format_instructions}

예시 형태:
{{
  "issue": "<input issue와 동일>",
  "citations": [
    {{
      "source_type": "statute",
      "title": "구 국제조세조정법 제5조 제1항 제6호",
      "citation_text": "구 국제조세조정법 제5조 제1항 제6호에 따르면 ..."
    }},
    {{
      "source_type": "case",
      "title": "대법원 2017두12345 판결",
      "citation_text": "대법원 2017두12345 판결은 ..."
    }}
  ]
}}

=================================================
[ISSUE]
{issue}

[TEXT]
{full_text}

"""


# BLOCK_ISSUE ="""

# 당신의 임무는 주어진 블록(full_block_text)이
# PASS1에서 도출된 main_issues 중 어떤 쟁점들과 관련되는지를 판별하는 것이다.

# ⚠ 절대 새로운 이슈를 만들어서는 안 된다.
# ⚠ main_issues 리스트에 없는 표현을 임의로 생성하지 마라.
# ⚠ 블록 전체를 기준으로 판단하라. 개별 문장 단위 판단 금지.

# =================================================
# [full_block_text]
# {full_block_text}

# [main_issues]
# {main_issues}

# =================================================
# 판단 규칙:

# 1) 블록 내부에 특정 issue와 관련된 사실, 주장, 법리, reasoning이 포함되면
#    그 issue는 해당 block에 "관련 있음"으로 판단한다.

# 2) 블록이 하나의 문장만 issue에 해당하더라도 "관련 있다"고 간주한다.
#    관련 여부는 강약이 아니라 존재 여부로 결정된다.

# 3) 블록 내용이 여러 issue에 걸쳐 있으면
#    관련된 모든 issue를 리스트로 포함시킨다.

# 4) 블록 내용이 주어진 어떤 이슈와도 직접적 관련이 없다면
#    빈 리스트([])를 출력한다.

# =================================================
# [출력 형식: JSON]
# {format_instructions}

# {{
#   "block_issues": ["<issue1>", "<issue2>", ...]
# }}

# JSON 외 어떤 문장도 출력하지 마라.


# """


# BRAVO_LOCAL_PROMPT = """

# 당신의 임무는 주어진 블록의 문장들을
# 법원의 논리 구조(reasoning_function)에 따라 분류하는 것이다.

# 이 블록은 이미 PASS2-A에서 특정 issue들과 연관된 것으로 판단되었으며,
# 해당 issue 목록(block_issues)은 모든 문장에 동일하게 상속된다.

# =================================================
# [block_issues]
# {block_issues}

# [full_block_text]
# {full_block_text}

# [sentences]
# {sentences}

# =================================================
# 각 문장을 다음 reasoning_function 중 하나로 분류하라:

# 1) premise  
#    - 법원이 판단의 출발점으로 삼는 사실·상황  
#    - 원고·피고 주장 중 법원이 인정한 사실  
#    - "이 사건에서…"와 같은 구조적 도입

# 2) evidence  
#    - premise를 뒷받침하는 자료, 진술, 문서, 정황  
#    - 사실 관계를 확인하는 근거 요소

# 3) rule  
#    - 법원이 언급하는 법령·규정·해석 기준  
#    - 법조문, 시행령, 시행규칙, 고시, 판례 등

# 4) application  
#    - rule을 이 사건 사실에 적용하는 법리 전개  
#    - “따라서 이 사건에서는 … 적용된다”  
#    - 구체적 사실에 법리를 대입하는 과정

# 5) inference  
#    - application 이후 법원이 도달하는 중간적 판단  
#    - 최종 결론 전 단계  
#    - “그러므로 ~~은 인정되기 어렵다” 등의 판단

# 6) mini_conclusion  
#    - 해당 block 또는 issue에서 법원이 내린 결론  
#    - 최종 판단 또는 쟁점 단위 판단의 마무리

# 7) background  
#    - 사건의 일반적 배경 설명  
#    - 법리 판단과 직접 연결되지 않는 주변 정보

# 8) irrelevant  
#    - 법리 판단과 연결되지 않는 문장  
#    - 태깅이 불가능한 경우 최소한 이 라벨로 지정

# =================================================
# 출력 형식(JSON):
# {format_instructions}
# {{
#   "tags": [
#     {{
#       "local_idx": 0,
#       "issue": ["<issue1>", ...], 
#       "function": "<reasoning_function>"
#     }},
#     {{
#       "local_idx": 1,
#       "issue": [...],
#       "function": "..."
#     }}
#   ]
# }}
# """